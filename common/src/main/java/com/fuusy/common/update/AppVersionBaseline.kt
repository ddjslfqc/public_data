package com.fuusy.common.update

/**
 * 当前发版基线，须与 [app/build.gradle] 中 defaultConfig 保持一致。
 * versionCode 变更时同步修改此处。
 */
object AppVersionBaseline {
    const val VERSION_CODE = 101
    const val VERSION_NAME = "1.0.1"
}
